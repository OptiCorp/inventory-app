import { styled } from 'styled-components'

export const InputWrap = styled.div`
    display: flex;
    flex-direction: row;
    margin: 0 auto;
`

export const ErrorP = styled.p`
    color: red;
    font-weight: 600;
    display: inline;
    padding: 1rem;
`
