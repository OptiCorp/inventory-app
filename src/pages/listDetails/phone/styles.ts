import { styled } from 'styled-components'

export const PhoneListTitle = styled.h2`
    font-weight: 600;
    line-height: 2rem;

    margin-inline: 20px;
`
export const PhoneContainer = styled.div`
    display: flex;
    flex-direction: column;
`
