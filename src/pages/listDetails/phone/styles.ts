import { styled } from 'styled-components';

export const PhoneListTitle = styled.p`
    font-weight: 600;
    margin-inline: 20px;
`;
export const PhoneContainer = styled.div`
    display: flex;
    flex-direction: column;
`;
